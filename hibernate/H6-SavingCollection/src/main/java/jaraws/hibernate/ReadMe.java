package jaraws.hibernate;

public class ReadMe {

	/*
	 * Saving Collections
	 * ---------------------------------------
	 * 
	 * So far we have seen that how a collection can be saved in database. In this example
	 * we will see how can we add an index or primary key column for the collection table
	 * column by defining @CollectionId annotation.
	 * 
	 * In this example we will see how can we save collection type objects in database
	 * via Hibernate.
	 * 
	 * The example we discussed in HibernateEg5 shows how a collection of Embeddable objects 
	 * can be saved in database
	 *
	 */
}

